# Example environment configuration for Augment Code Jira Integration
#
# SECURITY BEST PRACTICE:
# - Keep sensitive credentials in .env.local (git-ignored)
# - Keep non-sensitive config in .env (committed to git)
# - .env.local values override .env values

# For sensitive credentials, create .env.local with:
# JIRA_URL="https://your-domain.atlassian.net"
# JIRA_USERNAME="your-email@example.com"
# JIRA_API_TOKEN="your-jira-api-token"
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK"

# Non-sensitive Jira config (can be in .env)
JIRA_PROJECT="YOUR_PROJECT_KEY"

# Non-sensitive Slack config (can be in .env)
SLACK_CHANNEL="#your-channel"

# Instructions:
# 1. Create a Jira API token: https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Create a Slack webhook: https://api.slack.com/messaging/webhooks
# 3. Create .env.local with your sensitive credentials (this file is git-ignored)
# 4. Keep non-sensitive config in .env (this file is committed to git)
